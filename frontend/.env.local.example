# Required (no dev-time rewrite/proxy is used anymore — the app calls this URL directly).
# Local dev: http://localhost:5000
# Production: your Render backend URL, e.g. https://vpbms-backend.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:5000
